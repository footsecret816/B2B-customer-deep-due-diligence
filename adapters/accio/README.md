# Accio 平台适配原则

本文件只保留跨版本稳定的适配原则，不记录具体按钮位置。

- 把根目录 `SKILL.md` 作为客户背调核心规则。
- 如平台提供浏览器 / 网页操作能力，允许按固定 SOP 访问 LinkedIn、社媒、Maps 等页面。
- 需要登录的网站由用户在授权环境中完成登录；Skill 不保存账号密码。
- 业务 Agent 已掌握的公司知识通过 `BUSINESS_CONTEXT` 动态传入。
- 先跑公开客户画像，再用业务上下文做第二轮定向补充。
- 最终输出按 `CUSTOMER_PROFILE` 模板返回给上层业务 Agent。
