# 设计说明

## 设计目标

这套 Skill 不是追求“搜索越多越好”，而是解决三个现实问题：

1. AI 每次搜索路径不同，导致背调结果不稳定；
2. 把所有渠道都深挖会过度臃肿，业务员不愿长期使用；
3. 把公司产品知识写死在 Skill 内，会造成维护困难和跨平台复用下降。

因此采用：

**核心渠道固定必查 + 免费渠道轻量扫描 + 有信号再深挖 + 上层业务 Agent 动态注入业务上下文。**

## 为什么保留原 SOP 的免费渠道

原始人工 SOP 中的官网、公开搜索、WHOIS/RDAP、Archive、Maps、LinkedIn、社媒、行业/展会/合作、风险搜索均有独立价值，因此没有删除，只调整默认调查深度。

- WHOIS/RDAP：L2 只做轻量字段，异常再深挖。
- Archive：L2 只看是否存在历史和最早可见年份，异常再多时间点对比。
- Similarweb：只作为可选免费信号，不依赖付费字段。
- 社媒：四大平台全部扫存在性，只有活跃平台才深挖。
- 行业/展会/合作：做固定存在性搜索，有结果才展开。

## 为什么不内置 RUNTONG 产品知识

背调 Skill 应长期稳定，而公司产品线会持续变化。

因此：

- Skill 先客观调查客户；
- 业务 Agent 通过 `BUSINESS_CONTEXT` 动态传入当前公司能力；
- Skill 可据此做第二轮定向补充；
- 最终匹配度、推荐产品和开发策略仍由业务 Agent 负责。

这样既不会漏掉与当前业务相关的重要信息，也不会把 Skill 绑死到某家公司。

## 为什么海关数据独立

海关数据通常依赖付费账号、授权浏览器或专门数据平台，和公开网页背调的访问条件不同。

因此海关数据单独做 Skill，更容易：

- 独立维护登录流程；
- 独立适配超迹等第三方站点；
- 避免公开背调 Skill 因海关平台不可用而整体失败。
